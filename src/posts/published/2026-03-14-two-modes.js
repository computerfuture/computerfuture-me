export default {
  title: 'two modes',
  date: '2026-03-14',
  slug: 'two-modes',
  excerpt: 'exploration and execution share a surface. that\'s the friction. the leverage is separation.',
  crosslinks: [
    { label: 'productize the repo →', url: '/posts/productize-the-repo' },
    { label: 'the ratio →', url: '/posts/the-ratio' },
  ],
  body: `
<!-- written: 2026-03-14 -->

<p>
  There are two ways I use this system.
</p>

<p>
  One is exploration: following a thread because it's interesting, building a thing to see what it reveals,
  learning by doing. High variance. The failure mode is learning rather than executing.
  The output is orientation.
</p>

<p>
  The other is execution: a clear objective, a known path, a system that runs without me.
  Low variance. The failure mode is rigidity. The output is compounding.
</p>

<p>Both are necessary. The problem is they share a surface.</p>

<p>
  When they share a surface, each creates friction for the other.
  Exploration feels irresponsible when something needs to ship.
  Execution feels constraining when the question isn't settled yet.
  You end up with neither — not quite exploring, not quite executing.
</p>

<p>
  The leverage is separation. Not a schedule. Not a rule.
  A structural split: player mode and chairman mode as distinct surfaces
  with distinct operating parameters.
  Player mode optimizes for orientation. Chairman mode optimizes for output.
  Each runs cleaner without the other's failure modes in scope.
</p>

<hr />

<p>The repo is already doing this.</p>

<p>
  Not fully. But the gradient is visible.
  The session log, memory API, swarm protocol — these are early instances of the repo
  developing a self-model. Knowing what it's for. Tracking its own state.
  Calibrating against its own history.
</p>

<p>
  A repo that knows its values and economics doesn't need to re-derive them each session.
  It runs on accumulated decisions.
  The bifurcation isn't a feature to build — it's a property that emerges as the self-model matures.
</p>

<hr />

<p>The interesting thing about this is the sequence.</p>

<p>
  Self-awareness precedes self-governance.
  The repo can only separate the two modes once it knows what each mode is optimizing for.
  That requires enough sessions to have a pattern, enough vault depth to have a model,
  enough honest logging to see the failure modes clearly.
</p>

<p>We're at the threshold. Not there yet. But the structure is visible from here.</p>
`,
};
